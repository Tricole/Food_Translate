import { useEffect, useState, useRef } from "react";

//This does the processing from the photo, uses the functions required to translate the data, uses the functions from the allergy table and then provides feedback based on these
export default function DietFeedback(props) {
	return <></>;
}
